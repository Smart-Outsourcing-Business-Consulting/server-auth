Install this addon alongside `hr`. When an administrator edits an employee,
the **Related User** picker can select either an internal user or a portal user
from the employee's company.

Use the normal Odoo employee form to create or link the employee. This addon
does not run an employee-creation workflow.
